var apikey string
var locationid int
var interval = 10
var txpower = -77

stream
    |from()
        .measurement('telemetry')
    @infsoftwriter()
        .apikey(apikey)
        .locationid(locationid)
        .interval(interval)
        .txpower(txpower)
